<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.JammBell" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/bluapp</item>
        <item name="colorPrimaryDark">@color/bluappscuro</item>
        <item name="colorAccent">@color/bluapp</item>
        <!-- Customize your theme here. -->
    </style>
</resources>